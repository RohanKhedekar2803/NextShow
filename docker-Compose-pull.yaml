version: "3.1"

networks:
  nextshow-network:
    driver: bridge

volumes:
  mysql_data:

services:

  # ---------- Infrastructure Services ----------

  zookeeper:
    image: wurstmeister/zookeeper
    container_name: zookeeper
    mem_limit: 512m
    ports:
      - "${ZOOKEEPER_PORT}:${ZOOKEEPER_PORT}"
    networks:
      - nextshow-network

  kafka-server:
    image: wurstmeister/kafka
    container_name: kafka-server
    mem_limit: 2048m

    ports:
      - "${KAFKA_PORT}:${KAFKA_PORT}"
    environment:
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:${ZOOKEEPER_PORT}
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://${KAFKA_HOST}:${KAFKA_PORT}
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:${KAFKA_PORT}
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_HEAP_OPTS: "-Xms768m -Xmx1536m"
    depends_on:
      zookeeper:
        condition: service_started
    healthcheck:
      test: ["CMD", "bash", "-c", "echo > /dev/tcp/${KAFKA_HOST}/${KAFKA_PORT}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nextshow-network

  redis:
    image: redis:latest
    container_name: redis
    mem_limit: 256m
    ports:
      - "${REDIS_PORT}:${REDIS_PORT}"
    healthcheck:
      test: ["CMD", "redis-cli", "-h", "redis", "-p", "${REDIS_PORT}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nextshow-network

  mysql:
    image: mysql:8
    container_name: mysql
    mem_limit: 1536m
    restart: always
    ports:
      - "${MYSQL_EXPOSED_PORT}:${MYSQL_CONTAINER_PORT}"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init-multiple-dbs.sql:/docker-entrypoint-initdb.d/01-init-multiple-dbs.sql
      - ./dataseed.sql:/docker-entrypoint-initdb.d/02-dataseed.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "mysql", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nextshow-network

  # ---------- Microservices ----------

  discovery-service:
    image: rohankhedekar2803/nextshow-discovery-service:latest
    container_name: discovery-service
    mem_limit: 256m
    ports:
      - "${DISCOVERY_PORT}:${DISCOVERY_PORT}"
    environment:
      SERVER_PORT: ${DISCOVERY_PORT}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      JAVA_OPTS: "-Xms128m -Xmx192m"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://discovery-service:${DISCOVERY_PORT}"]
      interval: 15s
      timeout: 5s
      retries: 15
    networks:
      - nextshow-network

  gateway-service:
    image: rohankhedekar2803/nextshow-gateway-service:latest
    container_name: gateway-service
    mem_limit: 384m
    restart: always
    pull_policy: always
    ports:
      - "${GATEWAY_PORT}:${GATEWAY_PORT}"
    environment:
      SERVER_PORT: ${GATEWAY_PORT}
      FRONTEND_PROTOCOL: ${FRONTEND_PROTOCOL}
      FRONTEND_HOST: ${FRONTEND_HOST}
      FRONTEND_PORT: ${FRONTEND_PORT}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms128m -Xmx256m"
    depends_on:
      discovery-service:
        condition: service_healthy
      mysql:
        condition: service_healthy
    networks:
      - nextshow-network

  booking-service:
    image: rohankhedekar2803/nextshow-booking-service:latest
    container_name: booking-service
    restart: always
    pull_policy: always
    mem_limit: 384m
    ports:
      - "${BOOKING_PORT}:${BOOKING_PORT}"
    environment:
      SERVER_PORT: ${BOOKING_PORT}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_CONTAINER_PORT: ${MYSQL_CONTAINER_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      KAFKA_HOST: ${KAFKA_HOST}
      KAFKA_PORT: ${KAFKA_PORT}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      JWT_SECRET: ${JWT_SECRET}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms192m -Xmx320m"
    depends_on:
      mysql:
        condition: service_healthy
      kafka-server:
        condition: service_healthy
      redis:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
    networks:
      - nextshow-network

  events-service:
    image: rohankhedekar2803/nextshow-events-service:latest
    container_name: events-service
    restart: always
    pull_policy: always
    mem_limit: 384m
    ports:
      - "${EVENTS_PORT}:${EVENTS_PORT}"
    environment:
      SERVER_PORT: ${EVENTS_PORT}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_CONTAINER_PORT: ${MYSQL_CONTAINER_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      KAFKA_HOST: ${KAFKA_HOST}
      KAFKA_PORT: ${KAFKA_PORT}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      JWT_SECRET: ${JWT_SECRET}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms128m -Xmx256m"
    depends_on:
      mysql:
        condition: service_healthy
      kafka-server:
        condition: service_healthy
      redis:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
    networks:
      - nextshow-network

  shows-service:
    image: rohankhedekar2803/nextshow-shows-service:latest
    container_name: shows-service
    mem_limit: 384m
    restart: always
    pull_policy: always
    ports:
      - "${SHOWS_PORT}:${SHOWS_PORT}"
    environment:
      SERVER_PORT: ${SHOWS_PORT}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_CONTAINER_PORT: ${MYSQL_CONTAINER_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      KAFKA_HOST: ${KAFKA_HOST}
      KAFKA_PORT: ${KAFKA_PORT}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      JWT_SECRET: ${JWT_SECRET}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms128m -Xmx256m"
    depends_on:
      mysql:
        condition: service_healthy
      kafka-server:
        condition: service_healthy
      redis:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
    networks:
      - nextshow-network

  payments-notification-service:
    image: rohankhedekar2803/nextshow-payments-notification-service:latest
    container_name: payments-notification-service
    restart: always
    pull_policy: always
    mem_limit: 384m
    ports:
      - "${PAYMENTS_PORT}:${PAYMENTS_PORT}"
    environment:
      SERVER_PORT: ${PAYMENTS_PORT}
      FRONTEND_PROTOCOL: ${FRONTEND_PROTOCOL}
      FRONTEND_HOST: ${FRONTEND_HOST}
      FRONTEND_PORT: ${FRONTEND_PORT}
      NEXTSHOW_MAIL: ${NEXTSHOW_MAIL}
      SMTP_PORT: ${SMTP_PORT}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_CONTAINER_PORT: ${MYSQL_CONTAINER_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      KAFKA_HOST: ${KAFKA_HOST}
      KAFKA_PORT: ${KAFKA_PORT}
      JWT_SECRET: ${JWT_SECRET}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_PUBLIC_KEY: ${STRIPE_PUBLIC_KEY}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms192m -Xmx320m"
    depends_on:
      mysql:
        condition: service_healthy
      kafka-server:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
    networks:
      - nextshow-network

  user-service:
    image: rohankhedekar2803/nextshow-user-service:latest
    container_name: user-service
    mem_limit: 384m
    restart: always
    pull_policy: always
    ports:
      - "${USER_PORT}:${USER_PORT}"
    environment:
      SERVER_PORT: ${USER_PORT}
      FRONTEND_PROTOCOL: ${FRONTEND_PROTOCOL}
      FRONTEND_HOST: ${FRONTEND_HOST}
      FRONTEND_PORT: ${FRONTEND_PORT}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_CONTAINER_PORT: ${MYSQL_CONTAINER_PORT}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      KAFKA_HOST: ${KAFKA_HOST}
      KAFKA_PORT: ${KAFKA_PORT}
      NEXTSHOW_DOMAIN: ${NEXTSHOW_DOMAIN}
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET}
      DISCOVERY_HOST: ${DISCOVERY_HOST}
      DISCOVERY_PORT: ${DISCOVERY_PORT}
      JAVA_OPTS: "-Xms128m -Xmx256m"
    depends_on:
      mysql:
        condition: service_healthy
      discovery-service:
        condition: service_healthy
    networks:
      - nextshow-network

  # frontend:
  #   image: rohankhedekar2803/nextshow-frontend:latest
  #   container_name: nextshow-frontend
  #   mem_limit: 128m
  #   ports:
  #     - "5173:5173"
  #   depends_on:
  #     gateway-service:
  #       condition: service_started
  #     discovery-service:
  #       condition: service_healthy
  #   networks:
  #     - nextshow-network
  frontend:
    image: nginx:alpine
    container_name: nextshow-frontend
    restart: always
    pull_policy: always
    ports:
      - "80:80"
    volumes:
      - ./dist:/usr/share/nginx/html
      - ./nginx-frontend.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      gateway-service:
        condition: service_started
    networks:
      - nextshow-network
